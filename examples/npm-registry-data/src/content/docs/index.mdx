---
title: NPM registry data example
---

This page demonstrates a component that fetches data from the NPM package registry to display details about a given package. The content below is all generated from registry data. Try changing the `pkgName` prop in `index.mdx` to see details for a different NPM package.

---

import PackageDetails from '../../components/PackageDetails.astro'

<PackageDetails pkgName="@astrojs/starlight" />