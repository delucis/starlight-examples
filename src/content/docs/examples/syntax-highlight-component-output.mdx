---
title: Syntax highlight component output
description: Display the syntax-highlighted HTML a component generates using Starlight’s Code component
---

import StackBlitz from '../../../components/StackBlitz.astro';

This example demonstrates a custom `<HighlightOutput>` component which displays the HTML output of anything passed as children in a syntax-highlighted code block.
This makes it possible to display a component’s HTML markup to users for example.

<StackBlitz
	title="Syntax highlighting component output"
	description="Example of a custom component that outputs the HTML its children render instead of rendering the children directly, using Starlight’s <Code> component to syntax highlight the result."
	example="syntax-highlight-component-output"
	openFile="src/HighlightOutput.astro,src/content/docs/index.mdx"
/>
